fn Main() void {
	for (var page: u32 = 0; page < 32; page += 1) {
	    var addr: u32* = page*0x1000;
	    addr.* = page;
	}

	for (var page: u32 = 0; page < 32; page += 1) {
        var addr: u32* = page*0x1000;
        if (addr.* != page) while (true) {}
    }
	@susp();
}
