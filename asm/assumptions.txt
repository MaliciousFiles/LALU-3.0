assume nonzero $x; //Equiv to `assume boolean x == 0;`
assume boolean $x $> $y;
assume uninit $x;
assume init $x;
assume unreachable;
assume likely;
assume unlikely;
assume nonlocal $ptr;
assume atleastonce;
assume unique $ptr;